var sha = require('js-sha3').keccak512;

/**
 * User class
 * @param login
 * @param password
 * @param salt
 * @constructor
 */

function User(login, password, salt) {
    this.login = login;
    this.password = sha(password + salt);
    this.salt = salt;
}

module.exports = User;