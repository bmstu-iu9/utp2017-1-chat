var sha = require('js-sha3').keccak512;

function Session(sessionID, login, date) {
    //this.sessionID = sha(sessionID);
    this.sessionID = sessionID; //temporary stub
    this.login = login;
    this.date = date;
}

module.exports = Session;